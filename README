Бележки:

Концептуално ниво:
Системата генерира данни със шум, една част от които се използват за валидация -- тази част от системата наричаме forward model или оператор 
След синтезиране на данни, предстои да имплементираме inverse (защо не backward) model или оператор, който ще валидираме чрез golden-daтата